{
  "name": "event-scanner-subgraph",
  "version": "1.0.0",
  "description": "Universal event scanner subgraph for Arbitrum Sepolia",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/",
    "create-local": "graph create --node http://localhost:8020/ event-scanner",
    "remove-local": "graph remove --node http://localhost:8020/ event-scanner",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 event-scanner"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.82.0",
    "@graphprotocol/graph-ts": "^0.29.0"
  }
} 